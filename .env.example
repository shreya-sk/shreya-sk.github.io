# GitHub API Token (REQUIRED to avoid 403 rate limit errors!)
# Without token: 60 requests/hour → Blog will fail with 403 errors!
# With token: 5,000 requests/hour → Blog will work properly!
#
# HOW TO GET YOUR TOKEN:
# 1. Go to: https://github.com/settings/tokens
# 2. Click "Generate new token (classic)"
# 3. Give it a name like "blog-access-token"
# 4. Select scope: "public_repo" (for public repos) or "repo" (for private repos)
# 5. Click "Generate token"
# 6. Copy the token (starts with ghp_) and paste below
#
# SETUP:
# 1. Copy this file: cp .env.example .env.local
# 2. Edit .env.local and add your token
# 3. Rebuild: npm run build
VITE_GITHUB_TOKEN=

# Repository configuration
VITE_GITHUB_OWNER=shreya-sk
VITE_GITHUB_REPO=Knowledge-hub
