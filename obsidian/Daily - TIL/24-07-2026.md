# 24-07-2026

**SNYK FLAG, WRONG PATH**

Snyk flagged a high-severity CVE in a transitive dependency three levels deep. Before opening a ticket to bump it: checked whether the vulnerable *function* in that package was actually reachable from our code path at all - it wasn't, the affected export was never imported anywhere in the dependency chain we used.

Still worth bumping eventually, but it changed the priority from "drop everything" to "next sprint." Reading the actual advisory, not just the severity score, is the difference.
